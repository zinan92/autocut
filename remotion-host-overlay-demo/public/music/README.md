# Background Music

Put optional background music files here.

Default workflow rule:

- Keep BGM disabled unless the project explicitly needs it.
- Use low volume, usually around `0.04` to `0.06`.
- Keep speech intelligibility first.
- Place and mix BGM inside Remotion with `<Audio>` and `<Sequence>`.
